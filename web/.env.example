# Copy to .env and configure for your setup

# WebSocket bridge settings
VITE_WS_BRIDGE_URL=ws://localhost:8080

# Default memory file path (for display only, user must select via File System API)
VITE_DEFAULT_MEMORY_PATH=/tmp/haywire-vm-mem

# QMP settings (for bridge server)
QMP_HOST=localhost
QMP_PORT=4445